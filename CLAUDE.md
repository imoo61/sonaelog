# 作業ルール（Claude 用・毎セッション読み込み）

このリポジトリは防災備蓄管理 PWA。詳細な進め方は [開発プロセス.md](開発プロセス.md)、
変更の記録は [変更管理.md](変更管理.md)、プロジェクト背景は
[実装計画_家族共有.md](実装計画_家族共有.md) を参照。

## 変更は必ず4フェーズのループで進める

**要求整理 → 設計 → 実装 → テスト**（→ 必要なら該当フェーズへ戻る）

1. **要求整理**：要求・目的（利用者の価値）・受入基準を文章化。未決点を洗い出す
   → **G1: ユーザーに報告して合意を待つ**（Definition of Ready）
2. **設計**：データ構造／画面／後方互換／移行／リスク／テスト計画をまとめる
   → **G2: ユーザーに報告して OK を待つ**（自明な小変更は省略可。省略した旨を明記）
3. **実装**：小さいコミット、`VERSION` を更新、静的確認（`node --check` 等）
   → **G3: push した内容とバージョンを報告**（承認待ちは不要、テストへ進む）
4. **テスト**：Claude が静的確認、ユーザーへ実機テスト手順（手順＋期待結果）を渡す
   → **G4: 結果を報告**。合格なら [変更管理.md](変更管理.md) を「完了」にし関連ドキュメント更新。
     不合格ならバグの原因に応じて設計または実装へ戻る

## 勝手に進めず、必ず止まって確認すること

- データ形式の変更（localStorage キー、`settings` 構造、Firestore ドキュメント形、JSON 互換）
- 現行版 `index.html`（**凍結中**）への変更
- 他世帯・全ユーザーへ影響すること（Firestore ルール、無料枠／コスト、認証方式）
- プライバシー／セキュリティに関わること
- 明らかな正解がない UX 選択（実質的に異なる複数案があるとき）
- 元の要求を超える範囲拡大（スコープクリープ）
- 取り返しのつかない操作（データ削除、force push、履歴改変、公開範囲変更）
- 当初見積もりを大きく超えると分かったとき

→ これらは進めず、**選択肢＋推奨案＋理由**を出してユーザーの判断を待つ。

## 確認なしで進めてよいこと

- 明らかなバグ修正・タイポ／合意済み設計どおりの実装／外部挙動を変えない内部リファクタ
  ／`VERSION` 更新／ドキュメントの追随更新

## その他の約束

- 現行版（`/sonaelog/`）は原則さわらない。変更は `family/` 側で行う
- 1コミット＝1つのまとまった変更。コミットメッセージは日本語、末尾に Co-Authored-By 行
- push（デプロイ）はユーザーが求めたときのみ
- テスト結果は正直に。失敗は失敗と報告する

## 合言葉

- **「要望レビュー」**：`scripts/feedback-dump.json`（アプリの要望投稿を書き出したもの。
  ユーザーが `node scripts/pull-feedback.js` で更新）を読み、要約・重複整理して
  各要望を **CR化 / 見送り / 保留** に振り分けて提案する。実装には進まない。
  ファイルが無ければ、ユーザーにスクリプト実行を促す。
