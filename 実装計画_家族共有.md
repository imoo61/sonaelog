# 家族共有版 実装計画

## 位置づけ

| | URL | 役割 |
|---|---|---|
| 現行ローカル版（凍結） | `imoo61.github.io/sonaelog/` | 触らない。安定版 |
| 家族共有版（新規） | `imoo61.github.io/sonaelog/family/` | ここで開発・テスト |

- リポジトリは当面 `imoo61/sonaelog` のまま。うまくいったら中立名の GitHub Org へ移設（履歴は保持される）。
- 現行版のデータは各世帯で完結（LocalStorage）。「町内会に拡張」＝利用世帯を増やす配布の話であり、中央DBは持たない。
- 家族共有版は「1世帯の中で複数端末が同じ在庫を見る」ための同期のみを追加する。

## ファイル構成（`family/`）

| ファイル | 内容 |
|---|---|
| `index.html` | 現行UIのコピー＋同期レイヤー。Firebase 未設定なら自動でローカル専用モード |
| `manifest.json` / `sw.js` / `icon.svg` | 家族版専用（キャッシュ名・スコープを分離） |
| `firebase-config.js` | Firebase の設定。**18時以降にここへ実値を貼る**。値が「ここに」の間はローカル専用 |
| `firestore.rules` | テスト用（ログイン済みなら誰でも可） |
| `firestore.rules.production` | 本番用（世帯メンバーのみ）。第三者配布前に差し替え |

## データ設計（milestone1）

```
users/{uid}                     ← ログインのみ・世帯なしの人
  items: [ ... ]
  settings: { ... }

households/{householdId}        ← 世帯を作った/参加した人
  name: "○○の世帯"
  members: { uid: "owner" | "member", ... }
  inviteCode: "ABCD-2345"
  items: [ ... ]
  settings: { ... }
  updatedAt, updatedBy

inviteCodes/{CODE}              ← コード → 世帯ID の索引（参加時の逆引き用）
  householdId, createdBy
```

- **同期方式**：LocalStorage を画面表示の正とし、Firestore と双方向ミラー。
  - ローカル変更 → `saveItems` / `saveSettings` → `cloudPush()`（800ms デバウンス）
  - リモート変更 → `onSnapshot` → LocalStorage へ書き戻して再描画
- **既知の制限**：世帯データは 1 ドキュメント（items 配列まるごと）。同じ品を同時編集すると「後勝ち」。
  → 拡張時に「1品＝1ドキュメント（`households/{id}/items/{itemId}`）」へ変更予定。

## ユーザー体験

1. 設定タブ最上部に「👨‍👩‍👧 家族で共有（同期）」カード
2. 未設定：「この端末だけで動いています」＋現行版データ取り込みボタン
3. 設定済み・未ログイン：「Googleでログイン」＋「ログインしなければこの端末だけ」
4. ログイン済み・世帯なし：「世帯を作成」／「招待コードで参加」
5. 世帯あり：世帯名・メンバー数・最終更新・招待コード（コピー可）・「世帯から外す」「ログアウト」

## あなたの準備（18時〜、15分）

1. Google アカウントで https://console.firebase.google.com → プロジェクト作成（Analytics オフでOK）
2. 「Firestore Database」→ データベース作成 → **本番環境モード** → ロケーション `asia-northeast1`（東京）
3. 「Authentication」→ Sign-in method → **Google** を有効化
   - 承認済みドメインに `imoo61.github.io` を追加（テスト用に `localhost` も）
4. プロジェクト設定 → マイアプリ → **ウェブアプリを追加** → 表示された `firebaseConfig` を控える
5. その内容を `family/firebase-config.js` に貼る（私に渡してくれれば貼ります）
6. Firestore → ルール に `family/firestore.rules`（テスト用）を貼って「公開」

## マイルストーン1：自分の家族でテスト

- [ ] `family/` を push（GitHub Pages 反映）
- [ ] iPhone で `imoo61.github.io/sonaelog/family/` を開き「ホーム画面に追加」
- [ ] **最重要**：ホーム画面アイコンから起動 → 設定 → 「Googleでログイン」が通るか
  - 通らない場合の対策候補：ポップアップ方式へ変更／`signInWithRedirect` の戻り処理調整／auth ハンドラを別ドメインでホスト
- [ ] 「世帯を作成」→ 招待コード発行
- [ ] 家族の端末でコード参加 → 双方で在庫追加・編集が数秒で反映されるか
- [ ] 片方を機内モードにして編集 → 復帰後に同期されるか
- [ ] 現行版データ取り込みボタンの動作確認

## マイルストーン2：ハードニング（第三者配布前）

- [ ] `firestore.rules.production` へ差し替え、複数アカウントで越境アクセス不可を確認
- [ ] 1品＝1ドキュメント化（同時編集の衝突低減）
- [ ] 「世帯から抜ける」で members から uid を除去
- [ ] 招待コードの再発行（漏洩時の無効化）
- [ ] 中立名 GitHub Org へ移設＋配信先（Netlify / Cloudflare Pages / 独自ドメイン）決定
- [ ] プライバシーの一文（データは Firebase に保存される旨）

## リスクメモ

- iOS ホーム画面 PWA での Google サインインが最大の不確実要素（milestone1 で先に潰す）
- `firebase-config.js` はソースに見える（仕様上OK。守るのはルール）
- オーナーの Google プロジェクトが単一障害点。エクスポート/インポートを常設の避難経路に
- 無料枠（Spark）：家族利用なら余裕。町内一斉導入時のみ読み取り上限に注意 → 導入分散 or Blaze＋予算アラート
