# 開発プロセス

このアプリの変更を、一般的なアプリ開発のやり方に合わせて回すための手順書。
重い儀式にはしない。1件の変更要求を、小さな開発サイクル（要求→設計→実装→テスト）で
1周させることを基本とする。

Claude 向けの短縮版は [CLAUDE.md](CLAUDE.md)、変更の記録は [変更管理.md](変更管理.md)。

---

## 全体像

```
        ┌───────────────────────────────────────────────┐
        ↓                                               │
  ① 要求整理 ──G1──▶ ② 設計 ──G2──▶ ③ 実装 ──G3──▶ ④ テスト ──G4──▶ 完了
  (Requirements)     (Design)      (Implementation)   (Test)
        ↑                ↑              ↑                │
        └── 要求が変わった │  設計の穴 ────┘  実装のバグ ────┘
```

- **G1〜G4** はチェックポイント（ゲート）。G1・G2 はユーザーの合意を取ってから次へ進む。
- テストで問題が出たら、原因に応じて設計または実装へ戻る。要求自体が変わったら①からやり直す。
- 1サイクル＝1つの変更要求。大きな要求は複数の小さな要求に割る。

---

## ① 要求整理（Requirements）

**やること**

- 要求を1〜3行で言語化する（何を、なぜ ＝ 利用者にとっての価値）
- **受入基準**（Acceptance Criteria）を箇条書きにする ＝「これが満たされたら完成」と言える条件
- 対象を特定：どの版（現行版＝凍結／家族共有版）、影響する画面・データ
- 未決点・前提・確認が必要な点を洗い出す

**G1（Definition of Ready）**

次を満たしたらユーザーに報告し、合意を得てから設計へ進む：

- 要求と受入基準が文章になっている
- 未決点がすべて解消している（または「この前提で進める」の了解がある）
- 対象範囲が決まっている

→ [変更管理.md](変更管理.md) に新しい CR（Change Request）として起票する。

---

## ② 設計（Design）

**やること（必要な項目だけ）**

- **データ設計**：localStorage キー、`settings` 構造、Firestore ドキュメント形の変更点
- **後方互換**：古い JSON／既存データ／旧版↔新版 で壊れないか。壊れる場合の移行手順
- **UI**：どの画面に何を足す／変えるか
- **リスクとロールバック**：失敗したらどう戻すか
- **テスト計画**：実機で何を確認すれば受入基準を満たすと言えるか

**G2（設計レビュー）**

設計をユーザーに報告し、OK を得てから実装へ。
ただし **自明な小変更**（タイポ、明らかなバグ修正、合意済み設計の一部）は G2 を省略してよい。
その場合は「設計は自明なので省略」と明記する。

判断に迷う点・複数の妥当な案がある点は、ここで **選択肢＋推奨案＋理由** を出して待つ。

---

## ③ 実装（Implementation）

**やること**

- 合意した設計の範囲だけを実装する（設計外のことをやりたくなったら①へ戻す）
- 現行版 `index.html` は原則さわらない。変更は `family/` 側
- `VERSION` 文字列を更新する
- 小さいコミットに分ける。コミットメッセージは日本語、末尾に `Co-Authored-By` 行
- 静的確認：`node --check`（インライン JS を抽出して）、ブラウザでの読み込み確認、
  主要関数が定義されているか・例外が出ないか

**G3（実装完了報告）**

push した内容・`VERSION`・静的確認の結果を報告する。ここは承認待ち不要でテストへ進む。
push（＝GitHub Pages へのデプロイ）はユーザーが求めたときのみ行う。

---

## ④ テスト（Test / Verification）

**やること**

- Claude 側：静的確認、可能な範囲の自動チェック
- ユーザー側：**実機テスト手順**を渡す（操作手順＋期待される結果を1対1で）
- 家族共有機能は「2台で」確認する項目を必ず含める

**G4（Definition of Done）**

すべての受入基準が実機で確認できたら「完了」。
- [変更管理.md](変更管理.md) の該当 CR を「完了」にし、日付とコミットを記録
- 使い方・友人向け案内などの関連ドキュメントを更新
- 不合格なら、バグの性質に応じて設計（②）または実装（③）へ戻る。
  受入基準の見直しが必要なら要求整理（①）へ戻る

---

## 勝手に進めない（判断はユーザーに委ねる）

以下は Claude が単独で決めず、必ず止まって選択肢と推奨案を提示する：

| 分類 | 例 |
|---|---|
| データ形式の変更 | localStorage キー名、`settings` の構造、Firestore ドキュメント形、JSON 互換性 |
| 凍結対象への変更 | 現行版 `index.html` |
| 全体影響 | Firestore セキュリティルール、無料枠／課金、認証方式（Google/メール等） |
| プライバシー・安全 | 個人情報の扱い、公開範囲、外部送信 |
| UX の分岐 | 明らかな正解がなく、複数案が実質的に異なるとき |
| スコープ | 元の要求を超える追加、当初見積もりの大幅超過 |
| 不可逆操作 | データ削除、`git push --force`、履歴改変、リポジトリ公開設定 |

**確認なしで進めてよい**：明らかなバグ修正・タイポ、合意済み設計どおりの実装、
外部挙動を変えない内部リファクタ、`VERSION` 更新、ドキュメントの追随更新。

---

## 報告のしかた

- 各ゲートで、**結論を先に**書く（合意したいこと／OK が欲しいこと／完了報告）
- 長い経緯は繰り返さない。決まったことは再度議論しない
- テスト手順は「操作 → 期待結果」の形。曖昧語を避ける
- 失敗・未実施・スキップは、その旨をはっきり書く

---

## 用語

- **CR (Change Request)**：1件の変更要求。[変更管理.md](変更管理.md) で管理
- **受入基準 (Acceptance Criteria)**：完成の判定条件
- **Definition of Ready**：設計に進んでよい状態（G1）
- **Definition of Done**：完了と言える状態（G4）
