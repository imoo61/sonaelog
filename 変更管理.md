# 変更管理（Change Requests）

1件の変更要求 = 1 CR。[開発プロセス.md](開発プロセス.md) の4フェーズで回す。
新しい要求は下の「テンプレート」をコピーして一覧の先頭に追加する。

状態：`要求整理` / `設計` / `実装` / `テスト` / `完了` / `保留`

---

## 一覧

| CR | 概要 | 状態 | 更新日 |
|----|------|------|--------|
| CR-003 | アプリ内の要望投稿フォーム＋取得スクリプト | テスト待ち | 2026-09-08 |
| CR-002 | 保管場所をユーザーが編集できるように（世帯共有） | テスト待ち | 2026-09-08 |
| CR-001 | 家族共有版の新規開発（Firebase同期・世帯・招待コード） | 実機検証中 | 2026-09-08 |
| CR-000 | 現行ローカル版（凍結） | 完了 | — |

---

## CR-003 要望投稿フォーム＋取得スクリプト

- **状態**：テスト待ち（実機で未確認）
- **要求**：家族・友人がアプリへの要望・不具合を投稿できる場所を設定画面に置く。
  作者があとでまとめて確認できる
- **受入基準**
  - [ ] 設定画面から自由記述で要望を送信できる（ログイン済みのみ）
  - [ ] 保存されるのは本文・アプリバージョン・日時のみ（名前・uid 等は保存しない）
  - [ ] 送信後に確認メッセージが出て、入力欄がクリアされる
  - [ ] 一般ユーザーは他人の投稿も自分の投稿も読めない（read: if false）
  - [ ] 未ログイン／未設定端末では「ログインが必要」の表示のみ
  - [ ] `node scripts/pull-feedback.js` で `feedback` 全件を `scripts/feedback-dump.json` に書き出せる
  - [ ] サービスアカウント鍵・ダンプは git 管理外
- **設計メモ**：`feedback/{id} = {text, appVersion, createdAt}`。ルールは create のみ許可
  （3キー限定・1〜2000字）、read/update/delete は不可。取得は Admin SDK スクリプト。
  合言葉「要望レビュー」でトリアージ（CLAUDE.md に記載）
- **実装**：`family/index.html` `v2026.09.07-fam9` ／ `family/firestore.rules`(+.production)
  ／ `scripts/pull-feedback.js` ／ `scripts/README.md` ／ `package.json` ／ `.gitignore`
- **テスト結果**：（実機テスト待ち）

## CR-002 保管場所の編集機能

- **状態**：テスト待ち（実機で未確認）
- **要求**：保管場所が作者の家の間取り固定になっている。世帯ごとに自分の間取りへ変更したい
- **受入基準**
  - [ ] 設定画面で場所の追加・リネーム・絵文字変更・並べ替え・削除ができる
  - [ ] 「保存する」で確定し、世帯の他メンバーにも数秒で反映される
  - [ ] 備品追加のプルダウン、在庫「場所別」表示の並び順が追従する
  - [ ] `locations` の無い旧データ／旧JSONでも壊れない（既定10個にフォールバック）
  - [ ] 一覧から消した場所を使う在庫は消えず、編集時に選択肢が補完される
- **設計メモ**：`settings.locations = [{name,icon}]`。未設定なら `DEFAULT_LOCATIONS`。
  表示系は `getLocations()` 経由に統一。旧 `LOCATION_ICONS/ORDER` は撤去
- **実装**：`family/index.html` `v2026.09.07-fam8` / コミット `effdc3e`
- **テスト結果**：（実機テスト待ち）

## CR-001 家族共有版の新規開発

- **状態**：実機検証中（自分の家族で同期確認まで完了。友人配布は未）
- **要求**：各世帯は自分のデータを持ちつつ、家族間で在庫を共有したい。将来は町内へ配布
- **受入基準**
  - [x] 現行版とは別物として `family/` に新規。現行版は凍結
  - [x] Google ログイン（iOS ホーム画面 PWA で動くこと）
  - [x] 「世帯を作成」→ 招待コード →「コードで参加」で複数端末が同じ在庫を共有
  - [x] 双方向同期（片方の変更が数秒で相手に反映）
  - [x] 未ログインでもこの端末だけで全機能使える
  - [ ] 友人配布前に本番用 Firestore ルールへ差し替え、越境アクセス不可を確認
- **設計メモ**：[実装計画_家族共有.md](実装計画_家族共有.md)。LocalStorage を表示の正、
  Firestore と双方向ミラー。世帯データは1ドキュメント（同時編集は後勝ち＝要ハードニング）
- **主なコミット**：`3873849`（雛形）〜 `1387e93`（二重作成防止）ほか
- **既知の残課題**
  - 本番ルール未適用（テスト用のまま。ログインすれば他世帯が見える）
  - 1品1ドキュメント化（同時編集の衝突低減）＝マイルストーン2
  - 中立名 GitHub Org / 独自ドメインへの移設は「うまくいってから」

## CR-000 現行ローカル版

- **状態**：完了・凍結。`index.html`（リポジトリ直下）。以後さわらない
- 経緯は git ログ参照

---

## テンプレート（コピーして使う）

```
## CR-00X タイトル

- **状態**：要求整理
- **要求**：（何を、なぜ＝利用者の価値。1〜3行）
- **受入基準**
  - [ ] （完成の判定条件。実機で確認できる粒度で）
  - [ ] 
- **未決点 / 確認事項**：（G1 で解消してから設計へ）
- **設計メモ**：（データ／画面／後方互換／移行／リスク／テスト計画。G2 で合意）
- **実装**：（対象ファイル・VERSION・コミット）
- **テスト結果**：（実機での確認結果。合否と気づき）
```
